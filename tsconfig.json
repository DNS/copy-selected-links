{
    "compilerOptions": {
        "inlineSourceMap": false, // maximum js parsing performance
        "moduleResolution": "node",
        "strict": true,
        "target": "es2020" // compile as modernly as possible, parcel will downcompile anyway with babel and browserslist
    },
    "include": ["src/**/*.ts"]
}
